<Window x:Class="_9_DataGrid_Ordering.PopupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_9_DataGrid_Ordering"
        mc:Ignorable="d"
        Title="PopupView" Height="200" Width="200">
    <Grid>
        <StackPanel>
            <RadioButton Content="10" IsChecked="{Binding Check10, Mode=TwoWay}"/>
            <RadioButton Content="20" IsChecked="{Binding Check20, Mode=TwoWay}"/>
            <RadioButton Content="30" IsChecked="{Binding Check30, Mode=TwoWay}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Name="Okay" Margin="10, 0, 10, 0"  Content="Okay" Click="Okay_Click" />
                <Button Name="Cancel" Margin="10, 0, 10, 0" Content="Cancel" Click="Cancel_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
